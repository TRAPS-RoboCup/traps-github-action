name: Push tag

run-name: Push tag:${{ github.ref_name }}(${{ github.event.head_commit.message }})

on:
  push:

jobs:
  add-tag:
    runs-on: ubuntu-22.04

    permissions:
      contents: write

    steps:
      - name: Push tag
        uses: ${{ github.repository }}/push-tag@${{ github.sha }}
